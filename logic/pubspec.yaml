name: logic
description: Game logic for better_idle - pure Dart, no Flutter dependencies.
version: 1.0.0

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.6.0
  collection: ^1.19.0
  equatable: ^2.0.7
  intl: ^0.20.2
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
