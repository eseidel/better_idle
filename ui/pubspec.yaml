name: ui
description: "Melvor Idle, re-imagined for mobile."
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+2

environment:
  sdk: ^3.10.0

dependencies:
  async_redux: ^25.4.0
  collection: any
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  go_router: ^16.3.0
  intl: ^0.20.2
  logic:
    path: ../logic
  meta: any
  path_provider: ^2.1.5
  scoped_deps:
    git:
      url: https://github.com/shorebirdtech/shorebird.git
      path: packages/scoped_deps

dev_dependencies:
  fake_async: ^1.3.0
  flutter_test:
    sdk: flutter

  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
  assets:
    - shorebird.yaml
